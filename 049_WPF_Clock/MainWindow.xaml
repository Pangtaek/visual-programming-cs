<Window x:Class="_049_WPF_Clock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:_049_WPF_Clock"
        mc:Ignorable="d"
        Title="WPF_Clock" Height="350" Width="350">
    <StackPanel>
        <Menu>
            <MenuItem Header="보기">
                <MenuItem Header="아날로그" Click="Analog_Click"/>
                <MenuItem Header="디지털" Click="Digital_Click"/>
                <Separator/>
                <MenuItem Header="종료" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="옵션">
                <MenuItem Header="초 단위" Click="Sec_Click"/>
                <MenuItem Header="밀리초 단위" Click="MillSec_Click"/>
            </MenuItem>
        </Menu>
        <Grid Margin="10">
            <Canvas x:Name="canvas1" Width="250" Height="250">
                <Ellipse x:Name="analogClock" Width="250" Height="250"></Ellipse>
            </Canvas>
            <TextBlock x:Name="txtDate" FontFamily="맑은고딕" FontSize="15" Margin="15, 50" Foreground="BlueViolet"/>
            <TextBlock x:Name="txtTime" FontFamily="맑은고딕" FontSize="40" HorizontalAlignment="Center"
                       VerticalAlignment="Center" Foreground="RoyalBlue"/>
        </Grid>
    </StackPanel>
</Window>
